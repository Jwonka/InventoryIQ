<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
  <h1>Create Item</h1>
  <form th:action="@{/items}" th:object="${item}" method="post">
    <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>
    <label>SKU: <input th:field="*{sku}" /></label><br/>
    <label>Name: <input th:field="*{name}" /></label><br/>
    <label>Quantity: <input type="number" th:field="*{quantity}" min="0" /></label><br/>
    <button type="submit">Save</button>
  </form>
</body>
</html>